# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Tilapäiset Office-tiedostot (avoimena oleva tiedosto luo ~$-kopion)
~$*.docx
~$*.xlsx

# PowerPoint-pohja on osa repoa: templates/template.pptx (Git LFS).
# Muut .pptx/.potx (generoidut esitykset) eivät kuulu versionhallintaan.
*.pptx
*.potx
!templates/template.pptx

# Zip-paketit (generoidaan tarvittaessa, ei versionhallintaan)
*.zip

# Muut
*.tmp
*.log
